source 'https://rubygems.org'

gem 'sinatra'

# Rack is the interface between sinatra and the app
gem 'rack'

# Shotgun avoids restarting server when changes are made
gem 'shotgun'

# makes accessing the api easier
gem 'github_api'

# to load environment variables
gem 'dotenv'

gem 'byebug'
